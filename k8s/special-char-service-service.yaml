apiVersion: v1
kind: Service
metadata:
  name: special-char-service
  namespace: letter-image-generator
spec:
  selector:
    app: special-char-service
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
